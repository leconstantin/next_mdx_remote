import React from "react";

const Button = () => {
  return (
    <button className="py-1 px-4 bg-blue-600 rounded-lg text-white font-semibold">
      Button
    </button>
  );
};

export default Button;
